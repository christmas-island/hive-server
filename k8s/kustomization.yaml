apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: hive

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - ingress.yaml

commonLabels:
  app: hive
  version: v0.31.0
  managed-by: kustomize

images:
  - name: ghcr.io/christmas-island/hive-server
    newTag: v0.31.0
