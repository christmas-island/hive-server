apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: app-http

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - ingress.yaml

commonLabels:
  app: app-http
  version: v0.31.0
  managed-by: kustomize

images:
  - name: registry.digitalocean.com/christmas-island/app
    newTag: v0.31.0
